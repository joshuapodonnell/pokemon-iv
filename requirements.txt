# Core
pyautogui>=0.9.54
Pillow>=10.0.0
numpy>=1.24.0
mss>=9.0.0
pynput

# macOS / Apple Vision (M1 native OCR — best quality)
pyobjc-framework-Quartz>=9.0
pyobjc-framework-Vision>=9.0
pyobjc-framework-Cocoa>=9.0

# Fallback OCR (if PyObjC not available)
# pytesseract>=0.3.10  # also needs: brew install tesseract

# Database
# sqlite3 is stdlib — no install needed

pytesseract
requests
beautifulsoup4
flask
mlx-vlm